import { HidePackagePipe } from './hide-package.pipe';

describe('HidePackagePipe', () => {
  it('create an instance', () => {
    const pipe = new HidePackagePipe();
    expect(pipe).toBeTruthy();
  });
});
